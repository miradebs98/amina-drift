# amina-drift — frontend

Next.js analyst dashboard for the amina-drift KYC-drift detection system.

```bash
npm install
npm run dev                              # fixtures mode (default) — http://localhost:3000
NEXT_PUBLIC_DATA_MODE=live npm run dev   # use the FastAPI backend instead
```

See the root [README](../README.md) for the full system overview.
